// OAuth
require('dotenv').config();
const {
  generateRandomString,
  generateCodeChallenge,
} = require('../../client/helpers/helpers.js');

const authController = {};

authController.getCode = () => {};

module.exports = authController;
